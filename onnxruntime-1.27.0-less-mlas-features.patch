diff -Nuarp onnxruntime-1.27.0.orig/cmake/onnxruntime_mlas.cmake onnxruntime-1.27.0/cmake/onnxruntime_mlas.cmake
--- onnxruntime-1.27.0.orig/cmake/onnxruntime_mlas.cmake	2026-07-03 15:13:27.845000000 +0800
+++ onnxruntime-1.27.0/cmake/onnxruntime_mlas.cmake	2026-07-03 15:15:01.688499814 +0800
@@ -548,7 +548,7 @@ else()
         set_source_files_properties(${MLAS_SRC_DIR}/sqnbitgemm_kernel_neon_int8_i8mm.cpp
 				    PROPERTIES COMPILE_FLAGS " -march=armv8.2-a+i8mm ")
 
-        if (NOT APPLE)
+        if (FALSE)
           set(mlas_platform_srcs
             ${mlas_platform_srcs}
             ${MLAS_SRC_DIR}/aarch64/HalfGemmKernelNeon.S
diff -Nuarp onnxruntime-1.27.0.orig/onnxruntime/core/mlas/inc/mlas.h onnxruntime-1.27.0/onnxruntime/core/mlas/inc/mlas.h
--- onnxruntime-1.27.0.orig/onnxruntime/core/mlas/inc/mlas.h	2026-07-03 15:13:27.853000000 +0800
+++ onnxruntime-1.27.0/onnxruntime/core/mlas/inc/mlas.h	2026-07-03 15:15:01.689540913 +0800
@@ -87,7 +87,7 @@ Abstract:
 //
 
 #if defined(MLAS_TARGET_AMD64) || defined (MLAS_TARGET_POWER) || defined (MLAS_TARGET_ZVECTOR)
-#define MLAS_SUPPORTS_GEMM_DOUBLE
+// #define MLAS_SUPPORTS_GEMM_DOUBLE
 #endif
 
 #if (!defined(_MSC_VER)) || (_MSC_VER >= 1930)
@@ -98,7 +98,7 @@ Abstract:
 // When building an universial binary for APPLE, this flag would
 // cause trouble for x64 target.
 
-#define MLAS_F16VEC_INTRINSICS_SUPPORTED
+// #define MLAS_F16VEC_INTRINSICS_SUPPORTED
 
 #endif //
 #endif // ARM64
@@ -2005,7 +2005,7 @@ MlasHalfGemmConvertPackB(
     void* PackedB
     );
 
-#if defined(__aarch64__) && defined(__linux__)
+#if 0//defined(__aarch64__) && defined(__linux__)
 /**
  * @brief Whether current CPU supports Bfloat16(bf16) acceleration.
  */
diff -Nuarp onnxruntime-1.27.0.orig/onnxruntime/core/mlas/lib/mlasi.h onnxruntime-1.27.0/onnxruntime/core/mlas/lib/mlasi.h
--- onnxruntime-1.27.0.orig/onnxruntime/core/mlas/lib/mlasi.h	2026-07-03 15:13:27.861000000 +0800
+++ onnxruntime-1.27.0/onnxruntime/core/mlas/lib/mlasi.h	2026-07-03 15:15:01.690919893 +0800
@@ -403,7 +403,7 @@ size_t
 
 #else
 
-#if defined(__aarch64__) && defined(__linux__)
+#if 0//defined(__aarch64__) && defined(__linux__)
 typedef size_t(MLASCALL MLAS_SBGEMM_FLOAT_KERNEL)(
     const float* A,
     const bfloat16_t* B,
@@ -946,7 +946,7 @@ void
     const float* Bias,
     void* PackedB);
 
-#if defined(__aarch64__) && defined(__linux__)
+#if 0//defined(__aarch64__) && defined(__linux__)
 typedef
 bool
 (MLASCALL MLAS_SBGEMM_BATCH_OVERRIDE)(
@@ -1066,7 +1066,7 @@ extern "C" {
 #else
     MLAS_GEMM_FLOAT_KERNEL MlasSgemmKernelZero;
     MLAS_GEMM_FLOAT_KERNEL MlasSgemmKernelAdd;
-#if defined(__aarch64__) && defined(__linux__)
+#if 0//defined(__aarch64__) && defined(__linux__)
     MLAS_SBGEMM_FLOAT_KERNEL MlasSbgemmKernelZero;
     MLAS_SBGEMM_FLOAT_KERNEL MlasSbgemmKernelAdd;
 #endif
@@ -1094,7 +1094,7 @@ extern "C" {
     // AArch64 assembly micro-kernel for pointwise NCHWc convolution
     MLAS_CONV_POINTWISE_FLOAT_KERNEL MlasConvPointwiseFloatKernelNeonAsm;
 #endif
-#if defined(__linux__)
+#if 0//defined(__linux__)
     // AArch64 assembly fast-math micro-kernels
     MLAS_CONV_FLOAT_KERNEL MlasConvNchwBf16KernelNeon;
     MLAS_CONV_DEPTHWISE_FLOAT_KERNEL MlasConvDepthwiseBf16KernelNeon;
@@ -1280,7 +1280,7 @@ extern "C" {
 #define MLAS_QGEMM_THREAD_COMPLEXITY                65536
 #define MLAS_HGEMM_THREAD_COMPLEXITY                65536
 
-#if defined(__aarch64__) && defined(__linux__)
+#if 0//defined(__aarch64__) && defined(__linux__)
 #define MLAS_SBGEMM_THREAD_COMPLEXITY (size_t(64) * size_t(1024))
 #endif
 
@@ -1513,7 +1513,7 @@ struct MLAS_PLATFORM {
     // MLAS Conv overrides
     MLAS_CONV_PREPARE_FLOAT_OVERRIDE* MlasConvPrepareOverride = nullptr;
     MLAS_CONV_FLOAT_OVERRIDE* MlasConvOverride = nullptr;
-#if defined(__aarch64__) && defined(__linux__)
+#if 0//defined(__aarch64__) && defined(__linux__)
     // SBGemm overrides
     MLAS_SBGEMM_BATCH_OVERRIDE* MlasSBGemmBatchOverride = nullptr;
     MLAS_SBGEMM_PACK_B_SIZE_OVERRIDE* MlasSBGemmPackBSizeOverride = nullptr;
@@ -1556,7 +1556,7 @@ struct MLAS_PLATFORM {
     MLAS_CONV_FLOAT_KERNEL* ConvNchwcFloatKernel;
     MLAS_CONV_DEPTHWISE_FLOAT_KERNEL* ConvDepthwiseFloatKernel;
     MLAS_CONV_POINTWISE_FLOAT_KERNEL* ConvPointwiseFloatKernel;
-#if defined(__linux__)
+#if 0//defined(__linux__)
     MLAS_CONV_FLOAT_KERNEL* ConvNchwBf16Kernel;
     MLAS_CONV_DEPTHWISE_FLOAT_KERNEL* ConvDepthwiseBf16Kernel;
     MLAS_CONV_POINTWISE_FLOAT_KERNEL* ConvPointwiseBf16Kernel;
diff -Nuarp onnxruntime-1.27.0.orig/onnxruntime/core/mlas/lib/platform.cpp onnxruntime-1.27.0/onnxruntime/core/mlas/lib/platform.cpp
--- onnxruntime-1.27.0.orig/onnxruntime/core/mlas/lib/platform.cpp	2026-07-03 15:13:27.868000000 +0800
+++ onnxruntime-1.27.0/onnxruntime/core/mlas/lib/platform.cpp	2026-07-03 15:15:01.691618310 +0800
@@ -650,7 +650,7 @@ Return Value:
     this->ConvNchwcFloatKernel = MlasConvNchwcFloatKernelNeon;
     this->ConvDepthwiseFloatKernel = MlasConvDepthwiseFloatKernelNeon;
     this->ConvPointwiseFloatKernel = MlasConvPointwiseFloatKernelNeon;
-#if defined(__linux__)
+#if 0//defined(__linux__)
     this->ConvNchwBf16Kernel = MlasConvNchwBf16KernelNeon;
     this->ConvDepthwiseBf16Kernel = MlasConvDepthwiseBf16KernelNeon;
     this->ConvPointwiseBf16Kernel = MlasConvPointwiseBf16KernelNeon;
@@ -695,7 +695,7 @@ Return Value:
         this->MlasDynamicQGemmPackBOverride = ArmKleidiAI::MlasDynamicQGemmPackB;
         this->MlasConvPrepareOverride = ArmKleidiAI::MlasConvPrepare;
         this->MlasConvOverride = ArmKleidiAI::MlasConv;
-#if defined(__aarch64__) && defined(__linux__)
+#if 0//defined(__aarch64__) && defined(__linux__)
         // Currently only an SME2 variant of SBGEMM exists
         if (ArmKleidiAI::UseSME2){
             this->MlasSBGemmBatchOverride = ArmKleidiAI::MlasSBGemmBatch;
@@ -748,7 +748,7 @@ Return Value:
 
     const bool HasI8MMInstructions = MLAS_CPUIDINFO::GetCPUIDInfo().HasArmNeon_I8MM();
     if (HasI8MMInstructions) {
-#if defined(__linux__)
+#if 0//defined(__linux__)
 
         this->GemmU8U8Dispatch = &MlasGemmU8X8DispatchUmmla;
         this->GemmU8S8Dispatch = &MlasGemmU8X8DispatchUmmla;
diff -Nuarp onnxruntime-1.27.0.orig/onnxruntime/core/mlas/lib/sbgemm.h onnxruntime-1.27.0/onnxruntime/core/mlas/lib/sbgemm.h
--- onnxruntime-1.27.0.orig/onnxruntime/core/mlas/lib/sbgemm.h	2026-07-03 15:13:27.876000000 +0800
+++ onnxruntime-1.27.0/onnxruntime/core/mlas/lib/sbgemm.h	2026-07-03 15:14:47.056823399 +0800
@@ -30,7 +30,7 @@ Abstract:
         MLAS_SBGEMM_STRIDES Strides{128, 128, 256};
 --*/
 
-#if defined(__aarch64__) && defined(__linux__)
+#if 0//defined(__aarch64__) && defined(__linux__)
 
 #pragma once
 
diff -Nuarp onnxruntime-1.27.0.orig/onnxruntime/core/mlas/lib/sbgemm_kernel_neon.cpp onnxruntime-1.27.0/onnxruntime/core/mlas/lib/sbgemm_kernel_neon.cpp
--- onnxruntime-1.27.0.orig/onnxruntime/core/mlas/lib/sbgemm_kernel_neon.cpp	2026-07-03 15:13:27.883000000 +0800
+++ onnxruntime-1.27.0/onnxruntime/core/mlas/lib/sbgemm_kernel_neon.cpp	2026-07-03 15:14:47.056882179 +0800
@@ -15,7 +15,7 @@ Abstract:
 
 --*/
 
-#if defined(__aarch64__) && defined(__linux__)
+#if 0//defined(__aarch64__) && defined(__linux__)
 
 #include "arm_neon.h"
 #include "mlasi.h"
diff -Nuarp onnxruntime-1.27.0.orig/onnxruntime/core/mlas/lib/snchwc.cpp onnxruntime-1.27.0/onnxruntime/core/mlas/lib/snchwc.cpp
--- onnxruntime-1.27.0.orig/onnxruntime/core/mlas/lib/snchwc.cpp	2026-07-03 15:13:27.890000000 +0800
+++ onnxruntime-1.27.0/onnxruntime/core/mlas/lib/snchwc.cpp	2026-07-03 15:14:47.058337853 +0800
@@ -795,7 +795,7 @@ struct MLAS_NCHWC_CONV_NCHW_ALGORITHM :
 
 #if defined(MLAS_TARGET_AMD64) || defined(MLAS_TARGET_LARCH64) || (defined(MLAS_TARGET_ARM64) && defined(MLAS_USE_ARM_NEON_NCHWC)) || (defined(MLAS_TARGET_RISCV64) && defined(MLAS_USE_RVV))
         MLAS_CONV_FLOAT_KERNEL* Kernel = GetMlasPlatform().ConvNchwFloatKernel;
-#if defined(MLAS_USE_ARM_NEON_NCHWC) && defined(__linux__)
+#if 0//defined(MLAS_USE_ARM_NEON_NCHWC) && defined(__linux__)
         MLAS_CONV_FLOAT_KERNEL* const KernelFloat = GetMlasPlatform().ConvNchwFloatKernel;
         if (WorkBlock->UseBf16) {
             Kernel = GetMlasPlatform().ConvNchwBf16Kernel;
@@ -820,7 +820,7 @@ struct MLAS_NCHWC_CONV_NCHW_ALGORITHM :
                 &EffectiveKernelHeight);
 
             MLAS_CONV_FLOAT_KERNEL* KernelToUse = Kernel;
-#if defined(MLAS_USE_ARM_NEON_NCHWC) && defined(__linux__)
+#if 0//defined(MLAS_USE_ARM_NEON_NCHWC) && defined(__linux__)
             if (WorkBlock->UseBf16 &&
                 EffectiveKernelHeight == 3 &&
                 KernelWidth == 3) {
@@ -921,7 +921,7 @@ struct MLAS_NCHWC_CONV_POINTWISE_ALGORIT
         // output positions at once and significantly reduces memory traffic.
         MLAS_CONV_POINTWISE_FLOAT_KERNEL* const KernelFast = MlasConvPointwiseFloatKernelNeonAsm;
 #endif
-#if defined(MLAS_USE_ARM_NEON_NCHWC) && defined(__linux__)
+#if 0//defined(MLAS_USE_ARM_NEON_NCHWC) && defined(__linux__)
         if (WorkBlock->UseBf16) {
             Kernel = GetMlasPlatform().ConvPointwiseBf16Kernel;
         }
@@ -1074,7 +1074,7 @@ struct MLAS_NCHWC_CONV_DEPTHWISE_ALGORIT
 #if defined(MLAS_TARGET_ARM64) && defined(MLAS_USE_ARM_NEON_NCHWC) && !defined(_WIN32)
         MLAS_CONV_DEPTHWISE_FLOAT_KERNEL* const KernelFast = MlasConvDepthwiseFloatKernelNeonAsm;
 #endif
-#if defined(MLAS_USE_ARM_NEON_NCHWC) && defined(__linux__)
+#if 0//defined(MLAS_USE_ARM_NEON_NCHWC) && defined(__linux__)
         if (WorkBlock->UseBf16) {
             Kernel = GetMlasPlatform().ConvDepthwiseBf16Kernel;
         }
diff -Nuarp onnxruntime-1.27.0.orig/onnxruntime/core/providers/cpu/math/matmul.cc onnxruntime-1.27.0/onnxruntime/core/providers/cpu/math/matmul.cc
--- onnxruntime-1.27.0.orig/onnxruntime/core/providers/cpu/math/matmul.cc	2026-07-03 15:13:27.898000000 +0800
+++ onnxruntime-1.27.0/onnxruntime/core/providers/cpu/math/matmul.cc	2026-07-03 15:14:47.056931691 +0800
@@ -185,7 +185,7 @@ Status MatMul<double>::Compute(OpKernelC
   return Status::OK();
 }
 
-#if defined(__aarch64__) && defined(__linux__)
+#if 0//defined(__aarch64__) && defined(__linux__)
 bool GemmPackBBfloat16(AllocatorPtr& alloc,
                        const Tensor& tensor_b,
                        bool trans_a,
@@ -243,7 +243,7 @@ Status MatMul<float>::PrePack(const Tens
   // only pack Matrix B
   if (input_idx == 1) {
     size_t packed_b_size;
-#if defined(__aarch64__) && defined(__linux__)
+#if 0//defined(__aarch64__) && defined(__linux__)
     size_t dim1 = 0;
     size_t dim2 = 0;
     TensorShape b_shape = tensor.Shape();
@@ -322,7 +322,7 @@ Status MatMul<float>::Compute(OpKernelCo
   const size_t K = static_cast<size_t>(helper.K());
   const size_t lda = helper.Lda(trans_a);
   const size_t ldb = helper.Ldb(trans_b);
-#if defined(__aarch64__) && defined(__linux__)
+#if 0//defined(__aarch64__) && defined(__linux__)
   if (use_fastmath_mode_ && !trans_a && !trans_b && ((N * K) >= kFastMathModeKernelsizeThreshold)) {
     std::vector<MLAS_SBGEMM_DATA_PARAMS> data(max_len);
     for (size_t i = 0; i < max_len; i++) {
diff -Nuarp onnxruntime-1.27.0.orig/onnxruntime/core/providers/cpu/math/matmul.h onnxruntime-1.27.0/onnxruntime/core/providers/cpu/math/matmul.h
--- onnxruntime-1.27.0.orig/onnxruntime/core/providers/cpu/math/matmul.h	2026-07-03 15:13:27.905000000 +0800
+++ onnxruntime-1.27.0/onnxruntime/core/providers/cpu/math/matmul.h	2026-07-03 15:14:47.057001622 +0800
@@ -65,7 +65,7 @@ class MatMul<float> final : public OpKer
     trans_batch_a_ = trans_batch_a_attr != 0;
     trans_batch_b_ = trans_batch_b_attr != 0;
 
-#if defined(__aarch64__) && defined(__linux__)
+#if 0//defined(__aarch64__) && defined(__linux__)
     auto config_ops = info.GetConfigOptions().GetConfigEntry(kOrtSessionOptionsMlasGemmFastMathArm64Bfloat16);
     use_fastmath_mode_ = (config_ops == "1") && MlasBf16AccelerationSupported();
 #endif
@@ -97,7 +97,7 @@ class MatMul<float> final : public OpKer
 
   MLAS_BACKEND_KERNEL_SELECTOR_CONFIG mlas_backend_kernel_selector_config_;
 
-#if defined(__aarch64__) && defined(__linux__)
+#if 0//defined(__aarch64__) && defined(__linux__)
   // fastmath mode state
   bool use_fastmath_mode_;
   // sbgemm kernel is implemented as 8x8 blocks with weights pre-packed to 4 blocks of 4x2
