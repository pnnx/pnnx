diff -Nuarp vision-0.27.1.orig/CMakeLists.txt vision-0.27.1/CMakeLists.txt
--- vision-0.27.1.orig/CMakeLists.txt	2026-06-10 07:54:21.000000000 +0800
+++ vision-0.27.1/CMakeLists.txt	2026-07-03 15:18:20.393444060 +0800
@@ -25,6 +25,7 @@ if(WITH_MPS)
 endif()
 
 find_package(Torch REQUIRED)
+include_directories(${TORCH_INCLUDE_DIRS})
 
 if (WITH_PNG)
     add_definitions(-DPNG_FOUND)
@@ -83,8 +84,8 @@ include(GNUInstallDirs)
 include(CMakePackageConfigHelpers)
 
 set(TVCPP torchvision/csrc)
-list(APPEND ALLOW_LISTED ${TVCPP} ${TVCPP}/io/image ${TVCPP}/io/image/cpu ${TVCPP}/io/image/cpu/giflib ${TVCPP}/models ${TVCPP}/ops
-  ${TVCPP}/ops/cpu ${TVCPP}/io/image/cuda)
+list(APPEND ALLOW_LISTED ${TVCPP} ${TVCPP}/ops
+  ${TVCPP}/ops/cpu)
 if(WITH_CUDA)
     list(APPEND ALLOW_LISTED ${TVCPP}/ops/cuda)
 endif()
@@ -96,7 +97,7 @@ FOREACH(DIR ${ALLOW_LISTED})
     file(GLOB ALL_SOURCES ${ALL_SOURCES} ${DIR}/*.*)
 ENDFOREACH()
 
-add_library(${PROJECT_NAME} SHARED ${ALL_SOURCES})
+add_library(${PROJECT_NAME} STATIC ${ALL_SOURCES})
 target_link_libraries(${PROJECT_NAME} PRIVATE ${TORCH_LIBRARIES})
 
 if(WITH_MPS)
diff -Nuarp vision-0.27.1.orig/cmake/TorchVisionConfig.cmake.in vision-0.27.1/cmake/TorchVisionConfig.cmake.in
--- vision-0.27.1.orig/cmake/TorchVisionConfig.cmake.in	2026-06-10 07:54:21.000000000 +0800
+++ vision-0.27.1/cmake/TorchVisionConfig.cmake.in	2026-07-03 15:18:20.393389958 +0800
@@ -31,7 +31,9 @@ if(@WITH_CUDA@)
   target_compile_definitions(${PN}::${PN} INTERFACE WITH_CUDA)
 endif()
 
-find_package(Torch REQUIRED)
+if(NOT TARGET torch)
+  find_package(Torch REQUIRED)
+endif()
 target_link_libraries(${PN}::${PN} INTERFACE torch)
 
 if(@WITH_PNG@)
