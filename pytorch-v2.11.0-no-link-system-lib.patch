diff -Nuarp pytorch-v2.11.0.orig/cmake/TorchConfig.cmake.in pytorch-v2.11.0/cmake/TorchConfig.cmake.in
--- pytorch-v2.11.0.orig/cmake/TorchConfig.cmake.in	2026-03-24 02:40:42.000000000 +0800
+++ pytorch-v2.11.0/cmake/TorchConfig.cmake.in	2026-05-06 14:45:14.409392042 +0800
@@ -15,7 +15,7 @@
 #   torch
 macro(append_torchlib_if_found)
   foreach (_arg ${ARGN})
-    find_library(${_arg}_LIBRARY ${_arg} PATHS "${TORCH_INSTALL_PREFIX}/lib")
+    find_library(${_arg}_LIBRARY ${_arg} PATHS "${TORCH_INSTALL_PREFIX}/lib" NO_DEFAULT_PATH)
     if(${_arg}_LIBRARY)
       list(APPEND TORCH_LIBRARIES ${${_arg}_LIBRARY})
     else()
